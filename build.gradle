// Файл: /build.gradle (ОКОНЧАТЕЛЬНО ОБНОВЛЕННЫЙ)

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // УСТАНОВИТЕ АКТУАЛЬНУЮ ВЕРСИЮ для поддержки SDK 36
        classpath 'com.android.tools.build:gradle:8.11.1' 
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

