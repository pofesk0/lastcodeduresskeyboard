// Файл: /build.gradle (ОКОНЧАТЕЛЬНО ОБНОВЛЕННЫЙ)

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // УСТАНОВИТЕ АКТУАЛЬНУЮ ВЕРСИЮ для поддержки SDK 36
        classpath 'com.android.tools.build:gradle:6.2.0' 
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

